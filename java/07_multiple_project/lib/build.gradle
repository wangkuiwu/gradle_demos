apply plugin: 'java'

sourceSets {
    main {
        // 指定"源码路径"在src目录下
        java {
            srcDir 'src'
        }
        // 指定"资源路径"在resources目录下
        resources {
            srcDir 'resources'
        }
    }
}
